# 隐私和安全文件
.env
.env.local
.env.*.local
credentials.json
*.pem
*.key
auth.json
token.json
secrets.json

# Dependencies
node_modules/

# 临时文件
*.tmp
*.log
.DS_Store
Thumbs.db
temp/
dist/
.aios/*
!.aios/tasks/
!.aios/tasks/metrics/
!.aios/tasks/metrics/**
.rex-harness/
.contextdb-enable
site/
.venv-docs/

# 大文件（图片、视频）
images/
download/
cover-image.png
*.png
# 否定规则必须在 *.png 之后才能覆盖它
!aios/images/
!aios/images/**/*.png
!blog-site/assets/
!blog-site/assets/**/*.png
!docs-site/assets/
!docs-site/assets/**/*.png
*.jpg
*.jpeg
*.gif
*.mp4
*.mov

# 历史记录（可能包含敏感信息）
memory/history/
memory/context-db/
memory/workspace/

# Claude 本地配置
.claude/settings.local.json
.claude/config.json
.claude/commands/
.claude/worktrees/

# Worktrees
.worktrees/

# MCP 服务器
mcp-server/node_modules/
mcp-server/.npm-cache/
mcp-server/dist/
mcp-server/.browser-profiles/
mcp-server/.mcp.json
mcp-server/test-persistence.cjs

# 浏览器配置（包含登录状态）
.browser-profiles/

# MCP 配置文件
.mcp.json

# 小红书图片输出
xhs-images/

# skills 工具安装的各客户端目录
memory/skills/. */
memory/skills/skills-lock.json
memory/skills/skills/

# Codex CLI local artifacts (do not commit)
.codex/.personality_migration
.codex/config.toml
.codex/sessions/
.codex/shell_snapshots/
.codex/state_*.sqlite*

# Gemini CLI local artifacts (do not commit)
.gemini/settings.json

# Local analysis clones (do not commit)
.superpowers/
__pycache__/

# Legacy memo runtime state (do not commit)
memory/memo/
# Added by code-review-graph
.code-review-graph/

# Client-local MCP config — per-machine paths, not to be committed
.opencode.json
# (root opencode.json stays tracked: generic relative paths, shape asserted by
# scripts/tests/ecc-uplift.test.mjs)
.hermes/.aios-native-sync.json
.crush.json
crush.json

# MCP config backups created by codemap install/uninstall
.mcp.json.bak*
*.mcp.json.bak*
*.bak-*

# Client-local skill and config directories — managed by sync-skills.mjs
.claude/
.opencode/
.codex/
.gemini/
.grok/
.agents/

# Generated Skill certification/training run evidence. Release gates read
# `docs/evidence/skill-training/<skill>-certification-*/state.json` from git,
# so the canonical certify output (7 fixed filenames below) stays committable
# without `git add -f`; anything else dropped into these directories stays local.
docs/evidence/skill-training/*
!docs/evidence/skill-training/*/
docs/evidence/skill-training/*/*
!docs/evidence/skill-training/*/state.json
!docs/evidence/skill-training/*/tasks.json
!docs/evidence/skill-training/*/baseline-skill.md
!docs/evidence/skill-training/*/baseline.raw.json
!docs/evidence/skill-training/*/baseline.scored.json
!docs/evidence/skill-training/*/candidate.raw.json
!docs/evidence/skill-training/*/candidate.scored.json

# Bundled third-party install tree for the pptx sharing toolkit (rebuild with npm install)
pptx-ai-coding-share/node_modules/

# RL/实验运行产物（由测试和训练生成，不提交）
experiments/rl-shell-v1/campaigns/
experiments/rl-shell-v1/runs/
experiments/rl-shell-v1/shadow-evals/
experiments/rl-shell-v1/control-state/
experiments/rl-shell-v1/tasks/generated/
experiments/rl-shell-v1/configs/*.invalid-tasks.json
undefined/
.crush/
.skillopt/*
!.skillopt/rex-workflow-2026-07-17/
!.skillopt/rex-workflow-2026-07-17/**

# AIOS projected planning/superpowers skill links (absolute home paths; recreated by `aios plan project-skills`)
.hermes/
.codex/
.gemini/
.opencode/
.grok/
.claude/
.skillopt/
# WorkBuddy: only the generated skill projection is disposable; .workbuddy/memory is kept
.workbuddy/skills/
# Windows drive-letter path join artifacts (installer temp dirs written relative to cwd)
C:/

# mkdocs social plugin cache
.cache/

# Pencil design sources (kept out of repo per convention)
pen/
